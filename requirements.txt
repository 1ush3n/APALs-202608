# ============================================================
# APAL 调度系统依赖清单
# ============================================================
# 注意：PyTorch 必须通过官方索引安装以获取 CUDA 支持
# pip3 install torch torchvision --index-url https://download.pytorch.org/whl/cu130
# 然后再安装其余依赖：
# pip install -r requirements.txt
# ============================================================

torch>=2.0.0
torchvision>=0.15.0
torch_geometric>=2.4.0
gymnasium>=0.29.0
networkx>=3.0
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
tensorboard>=2.13
schedulefree>=1.0
openpyxl>=3.1
PyYAML>=6.0
